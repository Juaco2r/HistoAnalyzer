# Bundled classifiers — v1.0.4

Version 1.0.4 includes the three validated JSON classifiers in source, wheel and desktop builds. The GUI and worker use these resources automatically when no custom path is supplied. Custom classifiers remain supported and take precedence.
