# HistoAnalyzer v1.0.9 patch

Copy these files over an existing v1.0.8 repository, preserving their paths. This patch redirects InstanSeg model downloads to a writable user cache, reports the actual nuclei backend used, and disables IPython display attempts in frozen GUI workers.
